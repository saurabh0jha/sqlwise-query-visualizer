export enum ExportType {
  ALL_CSV = "all-csv",
  ALL_JSON = "all-json",
  SELECTION_CSV = "selection-csv",
  SELECTION_JSON = "selection-json",
}

export enum CopyType {
  ALL_CSV = "all-csv",
  ALL_JSON = "all-json",
  SELECTION_CSV = "selection-csv",
  SELECTION_JSON = "selection-json",
}
